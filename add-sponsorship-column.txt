-- إضافة عمود نوع الكفالة لجدول موظفي الفروع
-- نفّذ هذا في Supabase SQL Editor قبل نشر الكود على Render

ALTER TABLE branch_employees
  ADD COLUMN IF NOT EXISTS sponsorship_type text DEFAULT 'company';

-- للتأكد:
SELECT column_name, data_type, column_default
FROM information_schema.columns
WHERE table_name = 'branch_employees' AND column_name = 'sponsorship_type';
