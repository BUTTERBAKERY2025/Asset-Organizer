# Branch workspace extraction

- Source: `client/src/pages/branch-operations.tsx` (page JSX: heading, overview, actions, grouped 14-card board, navigation-only group and push-settings shell); `client/src/components/branch-operations/presentation.tsx` (copied into `presentation.tsx`, only import paths changed); `client/src/components/platform-app-icon.tsx` (copied into `platform-app-icon.tsx`); scoped relevant branch-ops rules and light-theme tokens from `client/src/index.css` in `../_group.css`, Cairo/Plus Jakarta Sans from `client/index.html`.
- Sandbox dependencies: local `lucide-react`, React, existing sandbox `@/components/ui/button` and `@/components/ui/select` (equivalent source UI components). No imports from the main app. The original `Layout` is intentionally replaced with a plain min-height wrapper; `MobilePushSettings` is a labelled offline placeholder.
- `fixtures.ts` exports the board type and `sampleBoard`/`sampleBranch`; `presentation.tsx` exports original `OperationCard`, `SectionMeta`, `SECTIONS`, `groupCards`, `requiredActions`, `partitionActions`, `isNavigationOnly`, `dailySalesProgress`, `formatServerDate` for redesign reuse.
- The fixture is **illustrative sample data, not a captured server response**. It mirrors the supplied Medina screenshot facts: no approved sales/target (unavailable, not zero), waste 0, 196 prior incomplete closures, 10 expired documents and partial module errors. Other metrics/errors, business date and oldest-closure date are synthetic; there are no names, personal records or live credentials. All 14 server-defined module IDs are present. Error cards expose no fabricated counts or alerts; document soon-expiring 0 is a sample assumption, not screenshot evidence.
- Auth/branch authorization and query fetch/refetch are replaced by this static fixture; branch picker has one disabled sample option, refresh and links show sandbox-only notices, with no API requests or app navigation. Push registration cannot run here. The original conditional loading/403/error states are excluded because this sample specifically represents the successfully loaded *partial* board.